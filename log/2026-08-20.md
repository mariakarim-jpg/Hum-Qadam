# 2026-08-20

**Done:**
- Worked through the Railway walkthrough with the user in real time — hit a
  hard paywall at the "New Project" step (confirmed by their screenshot,
  not assumed): Railway now requires upgrading to a paid plan just to
  create a project, no free path at all
- User declined to pay Railway, then also declined Render once it asked for
  a credit card at signup (never fully diagnosed why — user just doesn't
  want to hand over card details, which is a legitimate line to hold)
- User asked me to deploy directly using their Railway login link — declined
  or accepted account credentials/session links, explained why (no browser
  tool, and shouldn't accept login credentials regardless); offered a
  legitimate alternative (a scoped Railway API token) instead, which the
  user then also declined in favor of continuing the manual walkthrough
- Found and verified a genuinely card-free free host: **Vercel** — confirmed
  against their own docs that the Hobby plan has no billing cycle and a
  card is only requested if you explicitly choose to upgrade to Pro
- Adapted the backend to support Vercel's serverless model without
  breaking the existing Railway/Render path: split `server.js` into
  `src/app.js` (the actual Express app, no `.listen()`) + `src/server.js`
  (persistent-process entry point, unchanged behavior) + `api/index.js`
  (new Vercel serverless entry point, exports the same app)
- Verified, not assumed: re-booted `server.js` locally (unaffected, /health
  still 200), re-ran the full test suite (5/5 still passing), and wrote +
  ran a smoke test (`test/vercel-handler.smoke.js`) proving the exported
  handler works correctly with zero `app.listen()` calls anywhere in the
  process — the exact mechanism Vercel's runtime uses
- Rewrote `DEPLOYMENT.md` to present Vercel as Option C (recommended, no
  card), keeping Railway (Option A) and Render (Option B) documented for
  reference rather than deleting the earlier work

- Walked the user through the actual Vercel deployment live, step by step,
  with a lot of real friction along the way — Vercel Authentication blocking
  external requests (found via curl, not assumed), Root Directory not set
  correctly on first attempt (diagnosed from the `/var/task/backend/...`
  path in the crash log), frozen per-deployment URLs vs. the real stable
  production domain (`hum-qadam.vercel.app`, not the `hq4lq65q0` one — this
  tripped me up too, not just the user), and environment variables that
  looked "done" in conversation but weren't actually saved twice (frontend's
  first env var attempt, then the backend's Supabase vars) — caught each
  time by checking the real dashboard/logs/built bundle instead of trusting
  "done"
- Backend and frontend are now genuinely live and connected on Vercel:
  `https://hum-qadam.vercel.app` (backend) and
  `https://hum-qadam-de85.vercel.app` (frontend) — verified end-to-end by
  fetching the actual built JS bundle and confirming the real backend URL
  and real Supabase project URL are baked in, not placeholders
- Created a real Supabase project (`daqgyznxflaxhbyvdjne`), ran `schema.sql`
  (all 8 tables confirmed via Table Editor), added the user as the first
  coach row, fixed Supabase Auth's default `localhost:3000` Site URL (the
  cause of "localhost refused to connect" on the first magic-link attempt)
- User asked for a real coach self-registration flow (not just manual SQL)
  so other coaches/schools can sign up themselves. Built it: a lighter
  `requireSupabaseSession` auth middleware (proves "signed in," not
  "already a coach"), `GET /api/coaches/me` + `POST /api/coaches/register`
  (always uses the caller's own verified email, never client-supplied),
  and a `CompleteRegistration` page the frontend shows automatically the
  first time a new email signs in. This also closes a real gap I noticed
  along the way: Supabase Auth itself doesn't check our `coaches` table, so
  before this, any email could complete magic-link sign-in and only then
  discover every dashboard call 403'd
- Verified: backend tests still 5/5, new registration endpoints correctly
  401 with no token, wrong token, and missing token; frontend build succeeds
  cleanly with the new page and App.jsx routing logic

**Doing:**
- Committing the coach self-registration feature now

**Next:**
- Redeploy both Vercel projects with this change, then have the user
  actually test the new-coach registration flow with a second email
- Paste the loop-vs-hook reflection into an actual Google Doc and set sharing (still blocked on the Google Drive connector not being authorized for this session)
- Real Anthropic API key and Twilio credentials still not set — lesson
  plan generation and actual WhatsApp sending won't work until they are
- cron-job.org still not set up — the two scheduled jobs (morning check-in,
  evening reflection) won't fire on their own yet
- Start on Phase 1 roadmap work per `notes/planning.md` once deployment is fully settled
