# 2026-08-17

**Done:**
- Added `.claude/skills/daily-wrap-up/SKILL.md` — read today's notes, write a dated done/doing/next summary to `log/`
- Attempted a `/schedule` cloud routine (daily 6am PKT / `0 1 * * *` UTC) to run daily-wrap-up automatically — blocked: this project's GitHub account isn't connected to the scheduling system (`RemoteTrigger` returned a hard 401, not transient)
- Pivoted to a `PostToolUse` hook instead (`.claude/hooks/notes-wrapup-trigger.py`): fires whenever Claude Code writes/edits a file under `notes/`, nudging the model to run daily-wrap-up right then. Pipe-tested in isolation, then proved it end-to-end with a real matching write and a real non-matching control write, before cleaning up test artifacts and committing
- Drafted and pushed `notes/loop-vs-hook-reflection.md` — a one-page reflection comparing the two approaches, for the Google Doc submission
- Reviewed the repo for `planning.md`, `CLAUDE.md`, `skills/`, `agents/`, `decisions.md` — everything present except `planning.md` (intentionally deleted in the Memory Tune-Up)
- Recreated `notes/planning.md` — as a short index + current-status doc pointing at `plan/`, not a duplicate of it, to keep Decision 4 intact

- Scaffolded the full application per `plan/` + `CLAUDE.md`: `backend/` (Node/Express — conversation state machine matching `plan/08` state-for-state, all 5 `plan/09` AI prompts, six-component + banned-resource plan validator with passing unit tests, WhatsApp message-splitting, school-day/holiday logic, coach-scoped dashboard API, node-cron jobs) and `frontend/` (React/Vite coach dashboard covering all 5 `plan/14` pages)
- Verified rather than assumed: ran the validator's tests (5/5 pass), `npm install` + `npm run build` on the frontend (clean build), and actually booted the backend server in the background — found and fixed a real bug where `createClient()` crashed the whole process on a missing Supabase URL, then confirmed `/health` returns 200 and a webhook POST fails gracefully (logged, not crashed) without real credentials
- Updated root `README.md` to document the new `backend/`/`frontend/` layout and how to run both

- Prepped the repo for Railway deployment (one project, two services): added
  `backend/railway.toml` and `frontend/railway.toml`, wired a static file
  server into the frontend (`serve`, verified locally that it honors the
  `PORT` env var the same way Railway injects it), and tightened the
  backend's CORS from wide-open to a configurable `FRONTEND_URL` — verified
  both the allow-all fallback (unset) and the restricted case (set) actually
  work by booting the server with each and checking response headers
- Wrote `DEPLOYMENT.md` — full step-by-step walkthrough (backend first, since
  Vite bakes `VITE_*` vars at build time so the frontend needs the backend's
  URL before it can build correctly)

- User hit Railway's paywall (no real free tier anymore — trial credit only,
  then requires a paid Hobby plan) and asked to switch to a genuinely free
  host instead. Verified Render's actual free-tier terms via its docs before
  redesigning anything (free Web Services + Static Sites, no expiration —
  but free Web Services spin down after 15 min idle; free Static Sites don't)
- Redesigned scheduling around that constraint: moved job triggering out of
  in-process `node-cron` into two HTTP endpoints
  (`backend/src/routes/internal.js`, secret-protected) triggered by two
  external GitHub Actions workflows (`.github/workflows/`) instead — the
  external scheduler never sleeps, and its request wakes the backend when
  it's asleep. Hand-derived and documented the PKT->UTC cron conversion
  (including the weekday shift for the morning job, which crosses midnight)
- Verified this end-to-end by booting the server and hitting the new
  endpoint three ways: no secret (401), wrong secret (401), correct secret
  (ran, failed gracefully against the placeholder Supabase URL rather than
  crashing — same pattern as the earlier webhook test)
- Added `render.yaml` (Render Blueprint — creates both services in one step)
  and rewrote `DEPLOYMENT.md` end-to-end for Render instead of Railway;
  left `backend/railway.toml`/`frontend/railway.toml` in place but unused,
  noted as such, in case of a future move back to a paid always-on host

**Doing:**
- Committing the Render/GitHub-Actions redesign now

**Next:**
- Paste the loop-vs-hook reflection into an actual Google Doc and set sharing (still blocked on the Google Drive connector not being authorized for this session — not something I can do from here)
- Actually walk through the new `DEPLOYMENT.md` on Render — I have no Render account/API access from this session, so this one has to be done by hand
- Once deployed: set the two GitHub Actions secrets (`BACKEND_URL`, `JOB_TRIGGER_SECRET`) and manually run both workflows once to confirm green before trusting the schedule
- Fill in real Supabase/Anthropic/Twilio credentials and run `backend/src/db/schema.sql` against a real Supabase project before anything beyond `/health` will work end-to-end
- Start on Phase 1 roadmap work per `notes/planning.md` — conversation scripts, then pilot teacher recruitment (the scaffold is ahead of where the roadmap says to be — see the note left in my response to the user)
