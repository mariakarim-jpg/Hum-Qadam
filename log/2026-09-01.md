# 2026-09-01

**Done:**
- Installed Chrome DevTools MCP (`claude mcp add`) for real end-to-end
  browser testing — hit real friction getting it connected (config wrote
  correctly but needed a full VS Code restart before tools became
  available; `claude mcp list` never showed it even after, but the tools
  themselves worked once connected)
- Ran a real end-to-end test of the deployed coach dashboard
  (`hum-qadam-de85.vercel.app`) against the live backend, not a local copy
- Found the Supabase project had auto-paused (free-tier inactivity) —
  confirmed independently via both the browser and a direct `nslookup`
  against the machine's real DNS resolver ("Non-existent domain"), not
  assumed from a single source. User resumed it via the dashboard
- Found and fixed two real, confirmed bugs via actual browser interaction,
  not code review:
  1. Login never surfaced Supabase's `#error=...` redirect (expired/invalid
     link) — user saw a false "Check your inbox" success message.
     Fixed, verified against a local production build before deploying
  2. Direct navigation to any route but `/` (e.g. `/teachers`) returned a
     raw Vercel `404: NOT_FOUND`, not the app — missing SPA rewrite rule
     for the frontend (backend had one, frontend never did). Fixed,
     verified against the live deployment
- Hit Supabase's free-tier email limit (2 emails/hour, confirmed locked in
  the dashboard without custom SMTP) — magic-link auth burned through it
  fast during repeated testing, blocking every authenticated-flow check.
  Tried several workarounds first (admin "Send magic link" button — same
  limit; raising the rate-limit setting — locked; Admin API `generate_link`
  via curl — too much friction for the user to execute) before proposing
  the real fix
- Switched coach auth from magic-link to email+password after discussing
  the real trade-off with the user (coaches now manage a password; no
  longer capped at 2 logins/hour project-wide). Logged as Decision 5 in
  `notes/decisions.md`. Verified locally (build + serve + toggle between
  sign-in/sign-up modes) before deploying
- Chrome MCP browser connection itself was unstable multiple times mid-session
  (reverted to about:blank after clicks, then a full zombie-process lock)
  — diagnosed and fixed the second one myself by finding the exact stray
  `chrome-devtools-mcp`-profiled Chrome process via its command line and
  terminating only that one, without touching the user's real Chrome windows

- Found a third real bug while testing signup: the confirmation email is
  *also* subject to the same 2/hour cap, and a rate-limited confirmation
  email fails silently — `signUp()` only reports whether the account was
  created, not whether the email actually sent, so the app showed "Check
  your email" as if it had succeeded when no email was ever going to
  arrive. Reproduced myself (429 on `/auth/v1/signup`) to confirm before
  reporting it, not assumed from the user's report alone
- User decided to turn off "Confirm email" entirely rather than just fix
  the silent-failure UI — removes the email dependency at signup
  altogether instead of handling its failure better. Logged as Decision 6:
  the real trade-off (no identity proof at signup) is accepted because the
  `coaches` self-registration endpoint never grants access to real data by
  itself — a bogus signup gets an empty dashboard until someone explicitly
  assigns teachers to it

**Doing:**
- Waiting on the user to flip the "Confirm email" toggle off in Supabase's
  dashboard (Authentication → Sign In / Providers → Email), then verifying
  signup actually works instantly with no email step

**Next:**
- Once confirmed off: test a fresh signup myself, then get the user's own
  account working the same way
- Resume the flow-by-flow test once sign-in actually succeeds: Overview,
  self-registration (with a second email), Teacher List/Detail, Analytics,
  Reports — still genuinely untested
- Final report still owed: full ✅/❌/⚠️ summary across all 6 mapped flows
  once authenticated testing is actually complete
