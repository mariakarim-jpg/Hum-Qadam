# 2026-09-01

**Done:**
- Installed Chrome DevTools MCP (`claude mcp add`) for real end-to-end
  browser testing — hit real friction getting it connected (config wrote
  correctly but needed a full VS Code restart before tools became
  available; `claude mcp list` never showed it even after, but the tools
  themselves worked once connected)
- Ran a real end-to-end test of the deployed coach dashboard
  (`hum-qadam-de85.vercel.app`) against the live backend, not a local copy
- Found the Supabase project had auto-paused (free-tier inactivity) —
  confirmed independently via both the browser and a direct `nslookup`
  against the machine's real DNS resolver ("Non-existent domain"), not
  assumed from a single source. User resumed it via the dashboard
- Found and fixed two real, confirmed bugs via actual browser interaction,
  not code review:
  1. Login never surfaced Supabase's `#error=...` redirect (expired/invalid
     link) — user saw a false "Check your inbox" success message.
     Fixed, verified against a local production build before deploying
  2. Direct navigation to any route but `/` (e.g. `/teachers`) returned a
     raw Vercel `404: NOT_FOUND`, not the app — missing SPA rewrite rule
     for the frontend (backend had one, frontend never did). Fixed,
     verified against the live deployment
- Hit Supabase's free-tier email limit (2 emails/hour, confirmed locked in
  the dashboard without custom SMTP) — magic-link auth burned through it
  fast during repeated testing, blocking every authenticated-flow check.
  Tried several workarounds first (admin "Send magic link" button — same
  limit; raising the rate-limit setting — locked; Admin API `generate_link`
  via curl — too much friction for the user to execute) before proposing
  the real fix
- Switched coach auth from magic-link to email+password after discussing
  the real trade-off with the user (coaches now manage a password; no
  longer capped at 2 logins/hour project-wide). Logged as Decision 5 in
  `notes/decisions.md`. Verified locally (build + serve + toggle between
  sign-in/sign-up modes) before deploying
- Chrome MCP browser connection itself was unstable multiple times mid-session
  (reverted to about:blank after clicks, then a full zombie-process lock)
  — diagnosed and fixed the second one myself by finding the exact stray
  `chrome-devtools-mcp`-profiled Chrome process via its command line and
  terminating only that one, without touching the user's real Chrome windows

- Found a third real bug while testing signup: the confirmation email is
  *also* subject to the same 2/hour cap, and a rate-limited confirmation
  email fails silently — `signUp()` only reports whether the account was
  created, not whether the email actually sent, so the app showed "Check
  your email" as if it had succeeded when no email was ever going to
  arrive. Reproduced myself (429 on `/auth/v1/signup`) to confirm before
  reporting it, not assumed from the user's report alone
- User decided to turn off "Confirm email" entirely rather than just fix
  the silent-failure UI — removes the email dependency at signup
  altogether instead of handling its failure better. Logged as Decision 6:
  the real trade-off (no identity proof at signup) is accepted because the
  `coaches` self-registration endpoint never grants access to real data by
  itself — a bogus signup gets an empty dashboard until someone explicitly
  assigns teachers to it

- Confirmed email toggled off correctly (first attempt didn't actually
  save — confirmed via a real repro, not assumed). Once really off: tested
  a fresh signup end-to-end myself — instant session, no email at all,
  correctly routed into `CompleteRegistration`, then into a real, working
  Overview page with correct empty-state metrics
- Finished the full flow walkthrough with a real authenticated session:
  Teacher List (empty, correct), Analytics (all 4 charts render, no
  errors, though 3 of 4 show a blank area rather than a friendly empty
  state — noted as polish, not a bug), Reports (Generate Weekly Summary
  correctly shows "Internal server error" rather than crashing, since no
  Anthropic key is set yet — graceful failure confirmed), Sign out (works)
- Found and fixed a fourth issue: missing `id`/`name`/`aria-label` on
  Teacher List's two filter dropdowns (same class as the earlier Login.jsx
  fix). Verified the console warning is actually gone post-deploy —
  caught and corrected a browser-caching false negative first (checked
  network log showed the old JS bundle; confirmed via direct curl that
  the server was serving the new one correctly, then forced a
  cache-bypassing reload to get a true read)
- Verified existing-coach sign-in with a real password (not just signup)
  also works correctly end-to-end, and that a Supabase session survives a
  hard page reload
- Finished the full ✅/❌/⚠️ test report (6 flows, 4 bugs found/fixed) and
  published it as a shareable page (`test-evidence/submission-report.{md,html}`)
  alongside the before/after screenshot for the direct-navigation fix
- Set up a real daily scheduled E2E check (`HumQadam_MorningE2E`, Windows
  Task Scheduler, 6am PKT / 01:00 UTC) that runs `claude -p` headlessly
  against the live site via Chrome DevTools MCP and alerts on failure only.
  First attempt at "Claude Code routines" (`RemoteTrigger`/`/schedule`)
  hit an undocumented required field after several tries — abandoned
  rather than keep guessing at production infra, fell back to the OS
  scheduler as instructed
- Found and fixed a real bug in the schedule itself: it looked correctly
  configured but silently never fired — root cause was `schtasks`'
  `DisallowStartIfOnBatteries` default, blocking it while the laptop was
  unplugged. Proved the fix with genuine schedule-triggered runs (not
  hand-run): a clean pass, a deliberate failure (wrong URL) that correctly
  named the break and fired `PushNotification`, and a reverted pass that
  stayed silent
- Worked through the data-model module for the real project: guided the
  user through hand-drawing Hum Qadam's ERD step by step (didn't do it for
  them), then produced the final ERD, confirmed Part C (Supabase) and
  Part D (schema + env/gitignore/db-client) were already satisfied by
  existing project state rather than duplicating any of it — published as
  `notes/data-model-submission.html`

**Doing:**
- Nothing mid-flight — today's threads (testing, scheduling, data model)
  are each closed out and pushed

**Next:**
- Nothing blocking — remaining work is real product setup, not testing:
  real Anthropic/Twilio credentials, seeding real teacher data to test
  Teacher Detail (never reached — no teacher existed to click into), and
  deciding whether the Analytics empty-state polish is worth doing before
  wider use. The user still needs to grab one screenshot themselves
  (Supabase Table Editor — requires their own login) to finish the
  data-model submission
