# Railway service config for the frontend. Set this service's Root Directory
# to `frontend/` when creating it (monorepo — see root DEPLOYMENT.md).
[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
