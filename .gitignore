
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Backend / frontend (Node)
node_modules/
dist/
.env

# Scheduled E2E check — per-run logs are local evidence, not source
logs/e2e-scheduled/
